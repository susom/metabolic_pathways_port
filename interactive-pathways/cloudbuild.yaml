steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/frontend', './interactive-pathways']

  - name: 'gcr.io/cloud-builders/gcloud'
    args: ['app', 'deploy', '--image-url', 'gcr.io/$PROJECT_ID/frontend', '--quiet']

options:
  logging: CLOUD_LOGGING_ONLY
